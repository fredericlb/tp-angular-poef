/*
`baseVerbale` varchar(100) NOT NULL,
  `preterit` varchar(100) NOT NULL,
  `participePasse` varchar(100) NOT NULL,
  `traduction` varchar(100) NOT NULL,
 */
export class Verbe {
  id: number;
  baseVerbale: string;
  traduction: string;
}
